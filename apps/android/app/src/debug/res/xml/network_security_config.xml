<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug builds only. Replaces the release config so a developer build can reach
  an API running on the host machine over plaintext, while still refusing
  cleartext to anywhere else.

  10.0.2.2 is the host loopback as seen from the Android emulator. Add your
  machine's LAN address here to test from a physical device on the same Wi-Fi.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <!-- This machine's LAN address, for testing from a physical phone on
             the same Wi-Fi. Change it if your network hands out a new one:
             `ipconfig getifaddr en0`. Debug builds only. -->
        <domain includeSubdomains="false">192.168.1.60</domain>
    </domain-config>
</network-security-config>
